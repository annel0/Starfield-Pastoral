# Recipe System TODO (Parity-First)

状态：进行中（未完成）

## 已完成的骨架
- 已有玩家配方存储：`PlayerStardewData.unlockedRecipes`。
- 夜间升级触发点已接入：换日时会按升级结果调用 `applySkillLevelRecipeUnlocks(...)`。
- 升级页面已可读取技能等级解锁表并展示“新配方”文本。
- `skill_level_recipe_unlocks.json` 已可作为数据源被加载。

## 当前关键缺口（你说的“配方系统还没做”）
- 还没有完成原版 1:1 的“全量等级配方清单”核对与录入。
- 烹饪配方、制造配方、电视/好感/商店来源尚未统一到一个可验证的数据模型。
- 缺少“已解锁配方 -> 可制作/可见配方UI/配方来源提示”的完整闭环验收。
- 缺少自动化回归：升级当晚解锁、次日可制作、重复升级不重复发放。

## 按原版对齐的实施顺序
1. 数据层对齐
- 建立统一 schema：`sourceType`（skill/tv/friendship/shop/special）+ `sourceArgs`。
- 从原版数据逐条映射，生成可审计清单（含出处与差异注释）。

2. 运行时解锁对齐
- 夜间结算阶段只处理 `sourceType=skill` 的新增配方。
- 电视、好感、商店、特殊事件分别在对应时机触发解锁。

3. 展示与交互对齐
- LevelUp 页面显示“本级新增配方明细”（可多行）。
- 配方界面/烹饪界面读取同一套解锁状态，不做重复逻辑。

4. 验证与回归
- 用固定存档脚本覆盖 1~10 级每个技能的解锁断点。
- 校验与原版清单一致（配方ID、时机、是否重复发放）。

## 验收标准（Done Definition）
- 技能升级配方：与原版等级解锁 1:1 一致。
- 非技能来源配方：触发时机、条件与原版一致。
- 夜间流程：升级 -> 显示解锁 -> 数据落盘 -> 次日可用，全链路稳定。
- 回归测试：新增改动不会破坏已解锁配方状态。
